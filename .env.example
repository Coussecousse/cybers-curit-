# Configuration de l'application
NODE_ENV=development
PORT=3000

# Secret de session (GÉNÉRER UN NOUVEAU avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=votre_secret_de_session_ici

# Base de données
DB_PATH=./auth.db

# Configuration 2FA
APP_NAME=MonApplication
